# Jekyll build output and caches
_site/
.jekyll-metadata
.jekyll-cache/

# Bundler
Gemfile.lock
/vendor/
.bundle/

# Raw camera media (web-sized copies live in assets/images/montage)
images/PXL_*

# OS / editor artifacts
.DS_Store
*:Zone.Identifier
.vscode/
